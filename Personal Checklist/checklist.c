#include <stdlib.h>
#include <stdio.h>
#include <checklist.h>

int main(int argc, char const *argv[])
{
	// Checklist execution function
	// First Project

	/* If no args, start a new list */
	/* Function to list checklists */
	/* If arg, load list */
	/* Functions for new item, check item, delete item, display full list, display unchecked list */

	/* code */
	return 0;
}